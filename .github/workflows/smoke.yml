name: Smoke Test

on:
  push:
  workflow_dispatch:

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "✅ GitHub Actions runs on this repo (public = free)."
      - run: node --version && echo "runner OK"
