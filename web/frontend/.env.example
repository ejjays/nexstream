# full reference: docs/env-variables.md.

# backend base URL. for local dev the Vite proxy handles same-origin requests —
# leave unset. for a remote backend (tunnel, deployed instance), set the full URL.
VITE_API_URL=

# Sentry DSN for the frontend.
VITE_SENTRY_DSN=
