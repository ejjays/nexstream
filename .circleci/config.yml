version: 2.1

jobs:
  test-backend:
    docker:
      - image: cimg/node:22.0
      - image: cimg/redis:7.0
    steps:
      - checkout
      - restore_cache:
          keys:
            - v1-dependencies-{{ checksum "backend/package-lock.json" }}
            - v1-dependencies-
      - run:
          name: Install Backend Dependencies
          command: |
            cd backend
            # handle platform deps
            npm install
            # fix rollup/rolldown native binding bugs
            npm install @rollup/rollup-linux-x64-gnu @rolldown/binding-linux-x64-gnu
            # install yt-dlp for tests
            sudo curl -L https://github.com/yt-dlp/yt-dlp/releases/latest/download/yt-dlp -o /usr/local/bin/yt-dlp
            sudo chmod a+rx /usr/local/bin/yt-dlp

      - save_cache:
          paths:
            - backend/node_modules
          key: v1-dependencies-{{ checksum "backend/package-lock.json" }}
      - run:
          name: Setup YouTube Cookies
          command: |
            if [ -n "$YOUTUBE_COOKIES_BASE64" ]; then
              mkdir -p backend/temp
              echo "$YOUTUBE_COOKIES_BASE64" | base64 --decode > backend/temp/cookies.txt
              echo "youtube cookies injected from base64"
            fi
      - run:
          name: Run Backend Tests
          command: |
            cd backend
            npm test

workflows:
  version: 2
  build_and_test:
    jobs:
      - test-backend
