{
  "name": "nexstream-monorepo",
  "version": "1.0.0",
  "private": true,
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "scripts": {
    "ui": "cd frontend && npm run dev",
    "api": "cd backend && npm run dev",
    "build:ui": "cd frontend && npm install && npm run build",
    "build:api": "cd backend && npm install && npm run build",
    "lint": "echo 'linting changed (backend)...' && (cd backend && npm run lint) && echo 'linting changed (frontend)...' && (cd frontend && npm run lint) && echo '✅ lint passed (changed files)'",
    "lint:all": "echo 'linting backend...' && (cd backend && npm run lint:all) && echo 'linting frontend...' && (cd frontend && npm run lint:all) && echo '✅ lint passed (all files)'",
    "format:all": "prettier --write .",
    "format": "pretty-quick",
    "typecheck": "echo 'checking backend types...' && (cd backend && npx -y tsc --noEmit) && echo 'checking frontend types...' && (cd frontend && npx -y tsc --noEmit) && echo '✅ types passed'",
    "check": "npm run typecheck && npm run lint && echo '✅ all passed (changed lint)'",
    "check:all": "npm run typecheck && npm run lint:all && echo '✅ all passed (full lint)'",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^17.0.5",
    "prettier": "^3.8.3",
    "pretty-quick": "^4.2.2"
  }
}
